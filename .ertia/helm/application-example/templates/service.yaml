apiVersion: v1
kind: Service
metadata:
  name: {{ include "application-example.fullname" . }}
  labels:
    {{- include "application-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "application-example.selectorLabels" . | nindent 4 }}
