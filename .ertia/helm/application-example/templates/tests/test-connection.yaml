apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "application-example.fullname" . }}-test-connection"
  labels:
    {{- include "application-example.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "application-example.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
